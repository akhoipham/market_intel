name: Update dashboard

on:
  schedule:
    # On the hour, exactly, as requested. Note: GitHub's docs flag :00 as the
    # single most congested minute-mark for cron scheduling (every repo's
    # hourly/daily jobs target it too) — a marginally higher delay risk than
    # an offset minute, but the actual difference is small and unproven here.
    - cron: "0 * * * *"
    - cron: "0 6 * * 0"          # Weekly SEC ticker refresh (Sunday 06:00 UTC)
  workflow_dispatch: {}          # manual + external-cron trigger

permissions:
  contents: write

concurrency:
  group: update-dashboard
  cancel-in-progress: true   # a hung run must never permanently block the schedule

jobs:
  update:
    runs-on: ubuntu-latest
    timeout-minutes: 5   # normal runs take 30-55s; this bounds any hang hard
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Weekly ticker refresh (Sundays only)
        if: github.event.schedule == '0 6 * * 0'
        run: python run.py refresh-tickers

      - name: Fetch feeds and build dashboard
        run: python run.py fetch build

      - name: Commit updated data and dashboard
        run: |
          git config user.name "signal-desk-bot"
          git config user.email "actions@users.noreply.github.com"
          git add -A
          git commit -m "auto: refresh $(date -u +'%Y-%m-%d %H:%M UTC')" || { echo "nothing to commit"; exit 0; }
          # Retry push up to 5x; on conflict, take OUR generated files (bot wins
          # for the binary db/json, since a human never hand-edits those).
          for i in 1 2 3 4 5; do
            git pull --rebase -X ours --autostash && git push && exit 0
            echo "push attempt $i failed, retrying in 5s..."
            sleep 5
          done
          echo "::error::push failed after 5 attempts"
          exit 1
